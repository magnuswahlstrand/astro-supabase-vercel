---
import Layout from '../../layouts/Layout.astro';
import {getUser} from '../../auth';
const user = await getUser(Astro.request);

if (!user) {
    return Astro.redirect('/login');
}
---

<Layout title="About Supabase" user={user}>
    Something about Supabase
</Layout>
