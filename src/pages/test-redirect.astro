---
//const resp = Astro.redirect("/");
//resp.headers.set('Set-Cookie', `magnus=name; Path=/;`);

return new Response(null, {
    status: 302,
    statusText: 'Not found',
    headers: new Headers({
        'Set-Cookie': 'magnus=name; Path=/;',
        'Location': '/',
    })
});

return Response.redirect('https://www.google.com');
---
