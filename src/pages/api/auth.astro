---
console.log('AUTH !!!!!!!!!!');
import {supabase} from "../../auth"

const req = Astro.request;
const a = await Astro.request.text();
console.log(a);
const b = JSON.parse(a);
Astro.response.headers.set('Set-Cookie', `sa=${b.session.access_token}; Path=/;`);

// supabase.auth.api.setAuthCookie(Astro.request, Astro.response);
// import {logRequest} from "../../utils"
// await logRequest(req);
// import supabase from "../auth"


---

Something here
